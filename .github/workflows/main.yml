name: main
on:
  push:
    branches:
      - "**"
  workflow_dispatch:

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: "3.9"
      - uses: pre-commit/action@v3.0.0

  tests:
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10"]
        test-type: ["small", "large"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install jobsim
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          poetry install
      - name: run tests
        run: poetry run pytest -s -v --junitxml=${{ matrix.test-type }}_tests_results.xml tests/${{ matrix.test-type }}
      - uses: actions/upload-artifact@v3
        if: matrix.test-type == 'small'
        with:
          name: small_tests
          path: small_tests_results.xml
      - uses: actions/upload-artifact@v3
        if: matrix.test-type == 'large'
        with:
          name: large_tests
          path: large_tests_results.xml

  report_workflow:
    if: always()
    runs-on: ubuntu-latest
    needs: tests
    steps:
      - uses: uoboda-splunk/workflow-info-splunk-upload-action@v1
        with:
          splunk_host: ${{ secrets.SPLUNK_HOST }}
          splunk_token: ${{ secrets.SPLUNK_TOKEN }}
        env:
          GITHUB_TOKEN: ${{ github.token }}
